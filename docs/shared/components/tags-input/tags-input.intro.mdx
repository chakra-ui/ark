Tag inputs render tags inside an input, followed by an actual text input. By default, tags are added
when text is typed in the input field and the <kbd>Enter</kbd> or <kbd>Comma</kbd> key is pressed.
Throughout the interaction, DOM focus remains on the input element.
