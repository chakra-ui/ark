# Frame Angular Parity Audit

## Scope
- Angular files: `packages/angular/src/frame/frame.ts`, `packages/angular/src/frame/frame.stories.ts`, `packages/angular/src/frame/examples/basic.ts`, `packages/angular/src/frame/examples/script.ts`, `packages/angular/src/frame/examples/src-doc.ts`, `packages/angular/src/frame/examples/inherit-styles.ts`, `packages/angular/src/frame/frame.spec.ts`, `packages/angular/src/frame/public-api.ts`
- React files: `packages/react/src/components/frame/frame.tsx`, `packages/react/src/components/frame/frame-content.tsx`, `packages/react/src/components/frame/index.ts`
- Storybook/style files: `packages/react/src/components/frame/frame.stories.tsx`, `packages/react/src/components/frame/examples/basic.tsx`, `packages/react/src/components/frame/examples/script.tsx`, `packages/react/src/components/frame/examples/src-doc.tsx`, `packages/react/src/components/frame/examples/inherit-styles.tsx`, `packages/angular/src/frame/frame.stories.ts`, `packages/angular/src/frame/examples/*.ts`

## Summary
- Status: Re-audit confirms previous fixes still apply; no new functional, story, or styling gaps require Angular code changes this pass. Storybook visual verification remains deferred by unrelated repo-wide Storybook module resolution errors.
- Highest-risk gaps: Resolved in the prior audit pass. The iframe document is provided as the Ark environment to projected descendants via an embedded-view injector, the example styles drive the internal iframe via component-level fill styles, Basic uses `head` projection like React, and a spec proves environment propagation.

## Gap Matrix
| Area | Gap | React reference | Angular location | Fix |
| --- | --- | --- | --- | --- |
| Functionality | Children rendered inside the frame are wrapped in React `EnvironmentProvider` so descendants resolve the iframe document; Angular projected content used the parent injector and default document. | `packages/react/src/components/frame/frame.tsx` `EnvironmentProvider value={() => frameRef?.contentDocument ?? document}` | `packages/angular/src/frame/frame.ts` | Done in prior pass: create embedded content/head views with an injector that provides `ARK_ENVIRONMENT_TOKEN` with `getRootNode` returning the iframe document. Verified in `frame.spec.ts` `provides the iframe document as the Ark environment for projected descendants`. |
| Styling parity | React examples style the actual iframe. Angular examples style the component host, but the internal iframe did not reliably fill the host and kept browser default iframe styling. | React examples pass `style` directly to `<Frame>`/`iframe`. | `packages/angular/src/frame/frame.ts`, `packages/angular/src/frame/examples/*.ts` | Done in prior pass: component now ships `iframe { border: 0; display: block; height: 100%; width: 100% }` and host `display: block`. Examples set bordered host dimensions. |
| Story parity | React Basic demonstrates `head` injection for frame-local styles. Angular Basic used custom `srcdoc` CSS, leaving the `head` API covered only indirectly by tests. | `packages/react/src/components/frame/examples/basic.tsx` | `packages/angular/src/frame/examples/basic.ts` | Done in prior pass: Basic projects an Angular `ng-template #frameHead` with `<style>` matching the React example. |
| Story parity | React Storybook exports `Basic`, `Script`, and `SrcDoc`; Angular adds an `InheritStyles` story that backs the matching React example file (not exported as a React story). | `packages/react/src/components/frame/frame.stories.tsx`, `packages/react/src/components/frame/examples/inherit-styles.tsx` | `packages/angular/src/frame/frame.stories.ts` | No change: Angular keeps `InheritStyles` visible because the matching React example exists and it documents an Angular-specific style inheritance workflow. |
| Test parity | React has no component test file for Frame; Angular adds focused specs for rendering, srcdoc rewrite, head projection, environment propagation, server platform construction, and resize/unmount cleanup. | No React test file present under `packages/react/src/components/frame`. | `packages/angular/src/frame/frame.spec.ts` | Done in prior pass: 8 specs cover the Angular surface and the iframe environment guarantee for projected descendants. |
| Public API surface | React exports only `Frame`, `FrameBaseProps`, and `FrameProps`. Angular’s `public-api.ts` re-exports `initialFrameSrcdoc` because `frame.ts` exports it for internal effect-fallback use. | `packages/react/src/components/frame/index.ts` | `packages/angular/src/frame/public-api.ts` | No change: the constant mirrors React’s module-local `initialSrcDoc`. Treating it as a stable, opt-in helper preserves the directive-centric Angular surface and avoids forcing a second internal-only entry point per `docs/technical-requirements.md` §1 (entry points and "no namespace import"). Flagged here so a future trim is documented. |
| Public API surface | React `FrameProps` extends `IframeHTMLAttributes<HTMLIFrameElement>` so consumers pass arbitrary iframe attributes. Angular’s `FrameProps` exposes only `srcdoc`, `title`, `name`, plus `head`, `onMount`, `onUnmount`. | `packages/react/src/components/frame/frame.tsx` `FrameProps extends Assign<React.IframeHTMLAttributes<...>, FrameBaseProps>` | `packages/angular/src/frame/frame.ts` | No change: Angular’s directive-centric position in `docs/technical-requirements.md` §1 (selector prefix, signal `input()` per consumer-facing prop) prefers an explicit input list over HTML attribute spreading. Consumers needing other iframe attributes can wrap `<ark-frame>` and forward attributes onto the host. Flagged so this stays an intentional Angular-specific surface. |
| Lifecycle | React fires `onMount` exactly once per `FrameContent` mount and `onUnmount` on its unmount (incl. on srcDoc-driven remounts). Angular fires `mount` after each `mountFrame` call and `unmount` whenever previous views are torn down (incl. on `srcdoc` changes), matching React. | `packages/react/src/components/frame/frame-content.tsx`, `frame.tsx` | `packages/angular/src/frame/frame.ts` | No change: behavior already matches; covered by the destroy spec which proves `unmount` is emitted once per component destroy. |
| Title default | React seeds `title={\`frame:${useId()}\`}`. Angular seeds `frame:${crypto.randomUUID() ?? ++frameId}`. | `packages/react/src/components/frame/frame.tsx` `<iframe title={\`frame:${useId()}\`}>` | `packages/angular/src/frame/frame.ts` `createDefaultTitle` | No change: both produce a stable per-instance unique title and remain overridable by the `title` input; the Angular form is platform-safe and SSR-tolerant. |

## Implementation Plan
1. Re-audit complete. No additional code changes required this pass; both API and behavior matched React after the prior audit.
2. Confirm the existing fixes still pass `bun run --cwd packages/angular test:ci src/frame/frame.spec.ts`.
3. Confirm `bun run --cwd packages/angular typecheck` succeeds with the current Angular package state.
4. Document the re-audit and the explicit "No change" surface decisions above for future maintainers.

## Verification
- [x] Typecheck/build: `bun run --cwd packages/angular typecheck` succeeded (ran `tsc`, `ng build @ark-ui/angular --configuration=production`, `scripts/hide-private-entrypoints.ts`, and `scripts/check-forms-isolation.ts`). Existing ng-packagr export-condition warnings reproduced and are unrelated to Frame. A transient first run failed in `hide-private-entrypoints.ts` because the build output was still being written; the immediate re-run passed cleanly.
- [x] Component tests: `bun run --cwd packages/angular test:ci src/frame/frame.spec.ts` passed with 1 file and 8 tests, including the iframe environment propagation spec.
- [ ] Storybook render: Not re-attempted this pass. Prior attempt `bun run --cwd packages/angular storybook -- --port 6011 --ci` failed before serving due to unrelated stories importing unresolved package paths (`field/examples/custom-control.ts` and date-input/format locale examples). Frame stories were unaffected at compile but cannot be rendered until those siblings resolve.
- [ ] Manual/visual checks: Deferred because Storybook still does not compile for unrelated module resolution errors outside this component's scope.

## Commit
- Hash: Pending re-audit commit; previous parity work landed in `248540e20 fix(angular): align frame with react parity`.
- Message: `fix(angular): align frame with react parity`
