import { Checkbox, CheckboxControl, CheckboxInput, CheckboxLabel } from '@atlas/react'

export const ReactCheckbox = () => {
  return (
    <Checkbox>
      <CheckboxLabel>Checkbox</CheckboxLabel>
      <CheckboxInput />
      <CheckboxControl />
    </Checkbox>
  )
}
