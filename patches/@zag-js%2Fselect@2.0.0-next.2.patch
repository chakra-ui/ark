diff --git a/node_modules/@zag-js/select/.bun-tag-432c279044541be0 b/.bun-tag-432c279044541be0
new file mode 100644
index 0000000000000000000000000000000000000000..e69de29bb2d1d6434b8b29ae775ad8c2e48c5391
diff --git a/node_modules/@zag-js/select/.bun-tag-49f65ac67a8435b8 b/.bun-tag-49f65ac67a8435b8
new file mode 100644
index 0000000000000000000000000000000000000000..e69de29bb2d1d6434b8b29ae775ad8c2e48c5391
diff --git a/node_modules/@zag-js/select/.bun-tag-4d85b56f1b487802 b/.bun-tag-4d85b56f1b487802
new file mode 100644
index 0000000000000000000000000000000000000000..e69de29bb2d1d6434b8b29ae775ad8c2e48c5391
diff --git a/node_modules/@zag-js/select/.bun-tag-5ebb04bff23f0a42 b/.bun-tag-5ebb04bff23f0a42
new file mode 100644
index 0000000000000000000000000000000000000000..e69de29bb2d1d6434b8b29ae775ad8c2e48c5391
diff --git a/node_modules/@zag-js/select/.bun-tag-77238f9d6a70dfbb b/.bun-tag-77238f9d6a70dfbb
new file mode 100644
index 0000000000000000000000000000000000000000..e69de29bb2d1d6434b8b29ae775ad8c2e48c5391
diff --git a/node_modules/@zag-js/select/.bun-tag-8dac9a08e0941dcd b/.bun-tag-8dac9a08e0941dcd
new file mode 100644
index 0000000000000000000000000000000000000000..e69de29bb2d1d6434b8b29ae775ad8c2e48c5391
diff --git a/node_modules/@zag-js/select/.bun-tag-92c750eddb28bc0d b/.bun-tag-92c750eddb28bc0d
new file mode 100644
index 0000000000000000000000000000000000000000..e69de29bb2d1d6434b8b29ae775ad8c2e48c5391
diff --git a/node_modules/@zag-js/select/.bun-tag-a2940ee19bf5c2ae b/.bun-tag-a2940ee19bf5c2ae
new file mode 100644
index 0000000000000000000000000000000000000000..e69de29bb2d1d6434b8b29ae775ad8c2e48c5391
diff --git a/dist/align-with-trigger.js b/dist/align-with-trigger.js
index e372220c1e9968bdf9d533920faaa6fda1224615..b382ae51905e28b5bd565ca28a8427d698b3048b 100644
--- a/dist/align-with-trigger.js
+++ b/dist/align-with-trigger.js
@@ -147,10 +147,11 @@ function alignItemWithTrigger(options) {
   }
   const restorePositioner = captureStyles(positionerEl, POSITIONER_STYLE_KEYS);
   const restoreContent = captureStyles(contentEl, CONTENT_STYLE_KEYS);
+  positionerEl.style.setProperty("--reference-width", (0, import_utils.toPx)(triggerRect.width));
   positionerEl.style.position = "fixed";
   positionerEl.style.left = (0, import_utils.toPx)(clampedLeft);
   positionerEl.style.height = (0, import_utils.toPx)(height);
-  positionerEl.style.maxHeight = "auto";
+  positionerEl.style.maxHeight = "none";
   positionerEl.style.marginTop = (0, import_utils.toPx)(marginTop);
   positionerEl.style.marginBottom = (0, import_utils.toPx)(marginBottom);
   contentEl.style.height = "100%";
@@ -171,14 +172,38 @@ function alignItemWithTrigger(options) {
     positionerEl.style.bottom = "auto";
     const adjustedHeight = Math.max(0, height - contentOffsetTop);
     positionerEl.style.height = (0, import_utils.toPx)(adjustedHeight);
-    scroller.scrollTop = Math.max(0, scrollHeight - adjustedHeight);
+    // Pin to the end of the *resized* scroller. The original used the positioner height
+    // as a stand-in for the scroller height, which is smaller once padding and the
+    // overlaid arrows are accounted for, leaving mid-list items short of the trigger.
+    scroller.scrollTop = Math.max(0, scroller.scrollHeight - scroller.clientHeight);
   } else {
     positionerEl.style.top = "auto";
-    positionerEl.style.bottom = "0px";
+    const naturalHeight = positionerRect.height - scrollerRect.height + scrollHeight;
+    if (naturalHeight < height) {
+      positionerEl.style.bottom = (0, import_utils.toPx)(height - naturalHeight);
+      positionerEl.style.height = (0, import_utils.toPx)(naturalHeight);
+    } else {
+      positionerEl.style.bottom = "0px";
+    }
     scroller.scrollTop = scrollTopTarget;
   }
+  const styles = {};
+  for (const [key, value] of [
+    ["--reference-width", (0, import_utils.toPx)(triggerRect.width)],
+    ["position", positionerEl.style.position],
+    ["left", positionerEl.style.left],
+    ["top", positionerEl.style.top],
+    ["bottom", positionerEl.style.bottom],
+    ["height", positionerEl.style.height],
+    ["maxHeight", positionerEl.style.maxHeight],
+    ["marginTop", positionerEl.style.marginTop],
+    ["marginBottom", positionerEl.style.marginBottom]
+  ]) {
+    if (value !== "" && value != null) styles[key] = value;
+  }
   return {
     transformOrigin,
+    styles,
     cleanup() {
       restoreContent();
       restorePositioner();
@@ -214,8 +239,11 @@ function trackAlignItemWithTrigger(scope, options) {
     const marginTop = parseFloat(positionerStyles.marginTop) || 0;
     const marginBottom = parseFloat(positionerStyles.marginBottom) || 0;
     const viewportMax = win.document.documentElement.clientHeight - marginTop - marginBottom;
-    const contentMaxHeight = parseFloat((0, import_dom_query.getComputedStyle)(scroller).maxHeight);
-    const maxAvailableHeight = contentMaxHeight > 0 ? Math.min(viewportMax, contentMaxHeight) : viewportMax;
+    const readMaxHeight = (el) => {
+      const value = parseFloat((0, import_dom_query.getComputedStyle)(el).maxHeight);
+      return Number.isFinite(value) && value > 0 ? value : Number.POSITIVE_INFINITY;
+    };
+    const maxAvailableHeight = Math.min(viewportMax, readMaxHeight(scroller), readMaxHeight(contentEl));
     const scrollTop = scroller.scrollTop;
     const maxScrollTop = Math.max(0, scroller.scrollHeight - scroller.clientHeight);
     const diff = isTopPositioned ? maxScrollTop - scrollTop : scrollTop;
@@ -277,7 +305,7 @@ function trackAlignItemWithTrigger(scope, options) {
     if (fellBack) return;
     currentCleanup = result.cleanup;
     dom.getContentEl(scope)?.style.setProperty(TRANSFORM_ORIGIN_VAR, result.transformOrigin);
-    onAligned?.();
+    onAligned?.(result.styles);
   };
   const scheduleAlign = () => {
     if (disposed || fellBack || pendingFrame != null) return;
diff --git a/dist/align-with-trigger.mjs b/dist/align-with-trigger.mjs
index a7de71241583789c2e2d8f85de3b40e8c3a96bc9..611b1ae10564040a0312add1701f3d85bbde2238 100644
--- a/dist/align-with-trigger.mjs
+++ b/dist/align-with-trigger.mjs
@@ -113,10 +113,11 @@ function alignItemWithTrigger(options) {
   }
   const restorePositioner = captureStyles(positionerEl, POSITIONER_STYLE_KEYS);
   const restoreContent = captureStyles(contentEl, CONTENT_STYLE_KEYS);
+  positionerEl.style.setProperty("--reference-width", toPx(triggerRect.width));
   positionerEl.style.position = "fixed";
   positionerEl.style.left = toPx(clampedLeft);
   positionerEl.style.height = toPx(height);
-  positionerEl.style.maxHeight = "auto";
+  positionerEl.style.maxHeight = "none";
   positionerEl.style.marginTop = toPx(marginTop);
   positionerEl.style.marginBottom = toPx(marginBottom);
   contentEl.style.height = "100%";
@@ -137,14 +138,38 @@ function alignItemWithTrigger(options) {
     positionerEl.style.bottom = "auto";
     const adjustedHeight = Math.max(0, height - contentOffsetTop);
     positionerEl.style.height = toPx(adjustedHeight);
-    scroller.scrollTop = Math.max(0, scrollHeight - adjustedHeight);
+    // Pin to the end of the *resized* scroller. The original used the positioner height
+    // as a stand-in for the scroller height, which is smaller once padding and the
+    // overlaid arrows are accounted for, leaving mid-list items short of the trigger.
+    scroller.scrollTop = Math.max(0, scroller.scrollHeight - scroller.clientHeight);
   } else {
     positionerEl.style.top = "auto";
-    positionerEl.style.bottom = "0px";
+    const naturalHeight = positionerRect.height - scrollerRect.height + scrollHeight;
+    if (naturalHeight < height) {
+      positionerEl.style.bottom = toPx(height - naturalHeight);
+      positionerEl.style.height = toPx(naturalHeight);
+    } else {
+      positionerEl.style.bottom = "0px";
+    }
     scroller.scrollTop = scrollTopTarget;
   }
+  const styles = {};
+  for (const [key, value] of [
+    ["--reference-width", toPx(triggerRect.width)],
+    ["position", positionerEl.style.position],
+    ["left", positionerEl.style.left],
+    ["top", positionerEl.style.top],
+    ["bottom", positionerEl.style.bottom],
+    ["height", positionerEl.style.height],
+    ["maxHeight", positionerEl.style.maxHeight],
+    ["marginTop", positionerEl.style.marginTop],
+    ["marginBottom", positionerEl.style.marginBottom]
+  ]) {
+    if (value !== "" && value != null) styles[key] = value;
+  }
   return {
     transformOrigin,
+    styles,
     cleanup() {
       restoreContent();
       restorePositioner();
@@ -180,8 +205,11 @@ function trackAlignItemWithTrigger(scope, options) {
     const marginTop = parseFloat(positionerStyles.marginTop) || 0;
     const marginBottom = parseFloat(positionerStyles.marginBottom) || 0;
     const viewportMax = win.document.documentElement.clientHeight - marginTop - marginBottom;
-    const contentMaxHeight = parseFloat(getComputedStyle(scroller).maxHeight);
-    const maxAvailableHeight = contentMaxHeight > 0 ? Math.min(viewportMax, contentMaxHeight) : viewportMax;
+    const readMaxHeight = (el) => {
+      const value = parseFloat(getComputedStyle(el).maxHeight);
+      return Number.isFinite(value) && value > 0 ? value : Number.POSITIVE_INFINITY;
+    };
+    const maxAvailableHeight = Math.min(viewportMax, readMaxHeight(scroller), readMaxHeight(contentEl));
     const scrollTop = scroller.scrollTop;
     const maxScrollTop = Math.max(0, scroller.scrollHeight - scroller.clientHeight);
     const diff = isTopPositioned ? maxScrollTop - scrollTop : scrollTop;
@@ -243,7 +271,7 @@ function trackAlignItemWithTrigger(scope, options) {
     if (fellBack) return;
     currentCleanup = result.cleanup;
     dom.getContentEl(scope)?.style.setProperty(TRANSFORM_ORIGIN_VAR, result.transformOrigin);
-    onAligned?.();
+    onAligned?.(result.styles);
   };
   const scheduleAlign = () => {
     if (disposed || fellBack || pendingFrame != null) return;
diff --git a/dist/select.connect.js b/dist/select.connect.js
index 6e18a869997d8fa75c00226abf040912d5a2fc14..c0f8827c6c682a597631a8160014fb2ff1e44efc 100644
--- a/dist/select.connect.js
+++ b/dist/select.connect.js
@@ -68,6 +68,7 @@ function connect(service, normalize) {
   const selectedItems = computed("selectedItems");
   const currentPlacement = context.get("currentPlacement");
   const aligned = context.get("aligned");
+  const alignStyles = context.get("alignStyles");
   const currentPlacementSide = currentPlacement ? (0, import_popper.getPlacementSide)(currentPlacement) : void 0;
   const isTypingAhead = computed("isTypingAhead");
   const interactive = computed("isInteractive");
@@ -452,6 +453,7 @@ function connect(service, normalize) {
         style: {
           ...popperStyles.floating,
           transform: aligned ? "none" : popperStyles.floating.transform,
+          ...alignStyles ?? {},
           ...(0, import_dismissable.getDismissableLayerStyle)(layer, { zIndex: true })
         }
       });
diff --git a/dist/select.connect.mjs b/dist/select.connect.mjs
index 795e2b12153f7fae63b4866a4afedd1c15b25fd3..cf1b17fc6caacc5a715e42262d9d73e699fce926 100644
--- a/dist/select.connect.mjs
+++ b/dist/select.connect.mjs
@@ -46,6 +46,7 @@ function connect(service, normalize) {
   const selectedItems = computed("selectedItems");
   const currentPlacement = context.get("currentPlacement");
   const aligned = context.get("aligned");
+  const alignStyles = context.get("alignStyles");
   const currentPlacementSide = currentPlacement ? getPlacementSide(currentPlacement) : void 0;
   const isTypingAhead = computed("isTypingAhead");
   const interactive = computed("isInteractive");
@@ -430,6 +431,7 @@ function connect(service, normalize) {
         style: {
           ...popperStyles.floating,
           transform: aligned ? "none" : popperStyles.floating.transform,
+          ...alignStyles ?? {},
           ...getDismissableLayerStyle(layer, { zIndex: true })
         }
       });
diff --git a/dist/select.machine.js b/dist/select.machine.js
index f51b8a8621654d34c14ba92ded1424e198463a61..4b51101699bed6c6d0c76b62a1c742cf14544e5e 100644
--- a/dist/select.machine.js
+++ b/dist/select.machine.js
@@ -125,6 +125,9 @@ var machine = (0, import_core.createMachine)({
       })),
       aligned: bindable(() => ({
         defaultValue: false
+      })),
+      alignStyles: bindable(() => ({
+        defaultValue: null
       }))
     };
   },
@@ -530,7 +533,8 @@ var machine = (0, import_core.createMachine)({
           dir: prop("dir"),
           positioning,
           onPlacementChange,
-          onAligned() {
+          onAligned(styles) {
+            context.set("alignStyles", styles ?? null);
             context.set("aligned", true);
           }
         });
@@ -766,6 +770,7 @@ var machine = (0, import_core.createMachine)({
       clearPlacementState({ context }) {
         context.set("currentPlacement", void 0);
         context.set("aligned", false);
+        context.set("alignStyles", null);
       },
       startAutoScroll({ refs, scope, prop, event }) {
         const placement = event.placement;
@@ -859,7 +864,7 @@ var machine = (0, import_core.createMachine)({
         const label = collection2.stringifyItem(collection2.find(value));
         if (!label) return;
         const selected = context.get("value").includes(value);
-        const formatter = prop("translations").itemAnnouncement;
+        const formatter = prop("translations")?.itemAnnouncement;
         const message = formatter ? formatter({ value, label, selected }) : selected ? `${label}, selected` : label;
         refs.get("liveRegion")?.announce(message);
       },
diff --git a/dist/select.machine.mjs b/dist/select.machine.mjs
index d25f4b40b2fde058d4a8d7f18884fe52f7d0d347..20d839de33d3ec6c281c46cb3dc902d1d9226117 100644
--- a/dist/select.machine.mjs
+++ b/dist/select.machine.mjs
@@ -102,6 +102,9 @@ var machine = createMachine({
       })),
       aligned: bindable(() => ({
         defaultValue: false
+      })),
+      alignStyles: bindable(() => ({
+        defaultValue: null
       }))
     };
   },
@@ -507,7 +510,8 @@ var machine = createMachine({
           dir: prop("dir"),
           positioning,
           onPlacementChange,
-          onAligned() {
+          onAligned(styles) {
+            context.set("alignStyles", styles ?? null);
             context.set("aligned", true);
           }
         });
@@ -743,6 +747,7 @@ var machine = createMachine({
       clearPlacementState({ context }) {
         context.set("currentPlacement", void 0);
         context.set("aligned", false);
+        context.set("alignStyles", null);
       },
       startAutoScroll({ refs, scope, prop, event }) {
         const placement = event.placement;
@@ -836,7 +841,7 @@ var machine = createMachine({
         const label = collection2.stringifyItem(collection2.find(value));
         if (!label) return;
         const selected = context.get("value").includes(value);
-        const formatter = prop("translations").itemAnnouncement;
+        const formatter = prop("translations")?.itemAnnouncement;
         const message = formatter ? formatter({ value, label, selected }) : selected ? `${label}, selected` : label;
         refs.get("liveRegion")?.announce(message);
       },
