{
  "$schema": "https://turborepo.org/schema.json",
  "pipeline": {
    "build": {
      "outputs": [".next/**", "!.next/cache/**", "styled-system/**"],
      "dependsOn": ["^build"]
    },
    "lint": {},
    "test:ci": {},
    "typecheck": {}
  }
}
