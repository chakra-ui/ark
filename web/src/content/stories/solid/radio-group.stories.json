{
  "Basic": "import { For, createSignal } from 'solid-js'\nimport { Radio, RadioControl, RadioGroup, RadioGroupLabel, RadioLabel } from '@ark-ui/solid'\n\nconst Basic = () => {\n  const [value, setValue] = createSignal('apple')\n  return (\n    <RadioGroup onValueChange={({ value }) => setValue(value)}>\n      <RadioGroupLabel>Fruits: {value()}</RadioGroupLabel>\n      <For each={options}>\n        {(option) => (\n          <Radio\n            value={option.id}\n            style={{ 'font-weight': option.id === value() ? 'bold' : 'inherit' }}\n          >\n            <RadioLabel>{option.label}</RadioLabel>\n            <RadioControl />\n          </Radio>\n        )}\n      </For>\n    </RadioGroup>\n  )\n}",
  "Disabled": "import { For, createSignal } from 'solid-js'\nimport { Radio, RadioControl, RadioGroup, RadioGroupLabel, RadioLabel } from '@ark-ui/solid'\n\nconst Disabled = () => {\n  const [value, setValue] = createSignal('apple')\n  return (\n    <RadioGroup onValueChange={({ value }) => setValue(value)}>\n      <RadioGroupLabel>Fruits: {value()}</RadioGroupLabel>\n      <For each={options}>\n        {(option) => (\n          <Radio\n            value={option.id}\n            disabled={option.id === 'mango'}\n            style={{\n              color: option.id === 'mango' ? 'lightgray' : 'inherit',\n              'font-weight': option.id === value() ? 'bold' : 'inherit',\n            }}\n          >\n            <RadioLabel>{option.label}</RadioLabel>\n            <RadioControl />\n          </Radio>\n        )}\n      </For>\n    </RadioGroup>\n  )\n}"
}
