---
'@ark-ui/svelte': patch
---

Render `FileUpload.ItemGroup` as `ul`, `Menu.Separator` as `hr`, `Slider.ValueText` as `span`, and
`DatePicker.TableCellTrigger` and `Steps.List` as `div`, matching the element the other frameworks render.
