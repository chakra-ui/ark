{
  "Context": { "props": { "value": { "type": "UsePasswordInputReturn", "isRequired": true } } },
  "Control": {
    "props": {
      "asChild": {
        "type": "boolean",
        "isRequired": false,
        "description": "Use the provided child element as the default rendered element, combining their props and behavior."
      }
    }
  },
  "Indicator": {
    "props": {
      "asChild": {
        "type": "boolean",
        "isRequired": false,
        "description": "Use the provided child element as the default rendered element, combining their props and behavior."
      },
      "fallback": {
        "type": "string",
        "isRequired": false,
        "description": "The fallback content to display when the password is not visible."
      }
    }
  },
  "Input": {
    "props": {
      "asChild": {
        "type": "boolean",
        "isRequired": false,
        "description": "Use the provided child element as the default rendered element, combining their props and behavior."
      }
    }
  },
  "Label": {
    "props": {
      "asChild": {
        "type": "boolean",
        "isRequired": false,
        "description": "Use the provided child element as the default rendered element, combining their props and behavior."
      }
    }
  },
  "Root": {
    "props": {
      "asChild": {
        "type": "boolean",
        "isRequired": false,
        "description": "Use the provided child element as the default rendered element, combining their props and behavior."
      },
      "autoComplete": {
        "type": "'current-password' | 'new-password'",
        "isRequired": false,
        "description": "The autocomplete attribute for the input"
      },
      "defaultVisible": {
        "type": "boolean",
        "isRequired": false,
        "description": "Whether the password is visible by default"
      },
      "disabled": { "type": "boolean", "isRequired": false, "description": "Whether the input is disabled" },
      "id": { "type": "string", "isRequired": false, "description": "The unique identifier of the machine." },
      "ids": {
        "type": "Partial<{ input: string; visibilityTrigger: string }>",
        "isRequired": false,
        "description": "The ids of the elements in the password input. Useful for composition."
      },
      "ignorePasswordManagers": {
        "type": "boolean",
        "isRequired": false,
        "description": "Whether to ignore password managers"
      },
      "invalid": { "type": "boolean", "isRequired": false, "description": "Whether the input is in an invalid state" },
      "name": { "type": "string", "isRequired": false, "description": "The name attribute for the input" },
      "readOnly": { "type": "boolean", "isRequired": false, "description": "Whether the input is read-only" },
      "required": { "type": "boolean", "isRequired": false, "description": "Whether the input is required" },
      "translations": {
        "type": "Partial<{ visibilityTrigger: ((visible: boolean) => string) | undefined }>",
        "isRequired": false,
        "description": "Specifies the localized strings that identifies the accessibility elements and their states"
      },
      "visible": { "type": "boolean", "isRequired": false, "description": "Whether the password is visible" }
    }
  },
  "RootProvider": {
    "props": {
      "value": { "type": "PasswordInputApi<PropTypes>", "isRequired": true },
      "asChild": {
        "type": "boolean",
        "isRequired": false,
        "description": "Use the provided child element as the default rendered element, combining their props and behavior."
      }
    }
  },
  "VisibilityTrigger": {
    "props": {
      "asChild": {
        "type": "boolean",
        "isRequired": false,
        "description": "Use the provided child element as the default rendered element, combining their props and behavior."
      }
    }
  }
}
