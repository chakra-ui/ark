{
  "ClearTrigger": {
    "props": {
      "asChild": {
        "type": "(props: PropsFn<'button'>) => Element",
        "isRequired": false,
        "description": "Use the provided child element as the default rendered element, combining their props and behavior."
      },
      "render": {
        "type": "RenderFn<'button', EmptyState>",
        "isRequired": false,
        "description": "Render the part as a custom element, combining their props and behavior.\n\nCall `props()` to spread the part's props, passing your own to merge them rather than overwrite them.\nThe state arrives as an accessor:\n`render={(props, state) => <button {...props({ onClick: mine })}>{state().open}</button>}`"
      }
    }
  },
  "Control": {
    "props": {
      "asChild": {
        "type": "(props: PropsFn<'div'>) => Element",
        "isRequired": false,
        "description": "Use the provided child element as the default rendered element, combining their props and behavior."
      },
      "render": {
        "type": "RenderFn<'div', EmptyState>",
        "isRequired": false,
        "description": "Render the part as a custom element, combining their props and behavior.\n\nCall `props()` to spread the part's props, passing your own to merge them rather than overwrite them.\nThe state arrives as an accessor:\n`render={(props, state) => <button {...props({ onClick: mine })}>{state().open}</button>}`"
      }
    }
  },
  "Guide": {
    "props": {
      "asChild": {
        "type": "(props: PropsFn<'div'>) => Element",
        "isRequired": false,
        "description": "Use the provided child element as the default rendered element, combining their props and behavior."
      },
      "render": {
        "type": "RenderFn<'div', EmptyState>",
        "isRequired": false,
        "description": "Render the part as a custom element, combining their props and behavior.\n\nCall `props()` to spread the part's props, passing your own to merge them rather than overwrite them.\nThe state arrives as an accessor:\n`render={(props, state) => <button {...props({ onClick: mine })}>{state().open}</button>}`"
      }
    }
  },
  "HiddenInput": {
    "props": {
      "value": { "type": "string", "isRequired": true },
      "asChild": {
        "type": "(props: PropsFn<'input'>) => Element",
        "isRequired": false,
        "description": "Use the provided child element as the default rendered element, combining their props and behavior."
      },
      "render": {
        "type": "RenderFn<'input', EmptyState>",
        "isRequired": false,
        "description": "Render the part as a custom element, combining their props and behavior.\n\nCall `props()` to spread the part's props, passing your own to merge them rather than overwrite them.\nThe state arrives as an accessor:\n`render={(props, state) => <button {...props({ onClick: mine })}>{state().open}</button>}`"
      }
    }
  },
  "Label": {
    "props": {
      "asChild": {
        "type": "(props: PropsFn<'label'>) => Element",
        "isRequired": false,
        "description": "Use the provided child element as the default rendered element, combining their props and behavior."
      },
      "render": {
        "type": "RenderFn<'label', EmptyState>",
        "isRequired": false,
        "description": "Render the part as a custom element, combining their props and behavior.\n\nCall `props()` to spread the part's props, passing your own to merge them rather than overwrite them.\nThe state arrives as an accessor:\n`render={(props, state) => <button {...props({ onClick: mine })}>{state().open}</button>}`"
      }
    }
  },
  "Root": {
    "props": {
      "asChild": {
        "type": "(props: PropsFn<'div'>) => Element",
        "isRequired": false,
        "description": "Use the provided child element as the default rendered element, combining their props and behavior."
      },
      "defaultPaths": {
        "type": "string[]",
        "isRequired": false,
        "description": "The default paths of the signature pad.\nUse when you don't need to control the paths of the signature pad."
      },
      "disabled": { "type": "boolean", "isRequired": false, "description": "Whether the signature pad is disabled." },
      "drawing": {
        "type": "DrawingOptions",
        "defaultValue": "'{ size: 2, simulatePressure: true }'",
        "isRequired": false,
        "description": "The drawing options."
      },
      "ids": {
        "type": "ElementIds",
        "isRequired": false,
        "description": "The ids of the signature pad elements. Useful for composition."
      },
      "name": {
        "type": "string",
        "isRequired": false,
        "description": "The name of the signature pad. Useful for form submission."
      },
      "onDraw": {
        "type": "(details: DrawDetails) => void",
        "isRequired": false,
        "description": "Callback when the signature pad is drawing or the committed paths change.\n`paths` contains only committed strokes; use `currentPath` for the in-progress stroke."
      },
      "onDrawEnd": {
        "type": "(details: DrawEndDetails) => void",
        "isRequired": false,
        "description": "Callback when the signature pad is done drawing."
      },
      "paths": { "type": "string[]", "isRequired": false, "description": "The controlled paths of the signature pad." },
      "readOnly": { "type": "boolean", "isRequired": false, "description": "Whether the signature pad is read-only." },
      "render": {
        "type": "RenderFn<'div', SignaturePadRootState>",
        "isRequired": false,
        "description": "Render the part as a custom element, combining their props and behavior.\n\nCall `props()` to spread the part's props, passing your own to merge them rather than overwrite them.\nThe state arrives as an accessor:\n`render={(props, state) => <button {...props({ onClick: mine })}>{state().open}</button>}`"
      },
      "required": { "type": "boolean", "isRequired": false, "description": "Whether the signature pad is required." },
      "translations": {
        "type": "IntlTranslations",
        "isRequired": false,
        "description": "The translations of the signature pad. Useful for internationalization."
      }
    }
  },
  "RootProvider": {
    "props": {
      "value": { "type": "UseSignaturePadReturn", "isRequired": true },
      "asChild": {
        "type": "(props: PropsFn<'div'>) => Element",
        "isRequired": false,
        "description": "Use the provided child element as the default rendered element, combining their props and behavior."
      },
      "render": {
        "type": "RenderFn<'div', SignaturePadRootProviderState>",
        "isRequired": false,
        "description": "Render the part as a custom element, combining their props and behavior.\n\nCall `props()` to spread the part's props, passing your own to merge them rather than overwrite them.\nThe state arrives as an accessor:\n`render={(props, state) => <button {...props({ onClick: mine })}>{state().open}</button>}`"
      }
    }
  },
  "Segment": {
    "props": {
      "asChild": {
        "type": "(props: PropsFn<'svg'>) => Element",
        "isRequired": false,
        "description": "Use the provided child element as the default rendered element, combining their props and behavior."
      },
      "render": {
        "type": "RenderFn<'svg', EmptyState>",
        "isRequired": false,
        "description": "Render the part as a custom element, combining their props and behavior.\n\nCall `props()` to spread the part's props, passing your own to merge them rather than overwrite them.\nThe state arrives as an accessor:\n`render={(props, state) => <button {...props({ onClick: mine })}>{state().open}</button>}`"
      }
    }
  }
}
