{
  "Control": {
    "props": {
      "asChild": {
        "type": "(props: PropsFn<'div'>) => Element",
        "isRequired": false,
        "description": "Use the provided child element as the default rendered element, combining their props and behavior."
      },
      "render": {
        "type": "RenderFn<'div', EmptyState>",
        "isRequired": false,
        "description": "Render the part as a custom element, combining their props and behavior.\n\nCall `props()` to spread the part's props, passing your own to merge them rather than overwrite them.\nThe state arrives as an accessor:\n`render={(props, state) => <button {...props({ onClick: mine })}>{state().open}</button>}`"
      }
    }
  },
  "Indicator": {
    "props": {
      "asChild": {
        "type": "(props: PropsFn<'span'>) => Element",
        "isRequired": false,
        "description": "Use the provided child element as the default rendered element, combining their props and behavior."
      },
      "fallback": {
        "type": "number | boolean | Node | ArrayElement | (string & {})",
        "isRequired": false,
        "description": "The fallback content to display when the password is not visible."
      },
      "render": {
        "type": "RenderFn<'span', EmptyState>",
        "isRequired": false,
        "description": "Render the part as a custom element, combining their props and behavior.\n\nCall `props()` to spread the part's props, passing your own to merge them rather than overwrite them.\nThe state arrives as an accessor:\n`render={(props, state) => <button {...props({ onClick: mine })}>{state().open}</button>}`"
      }
    }
  },
  "Input": {
    "props": {
      "asChild": {
        "type": "(props: PropsFn<'input'>) => Element",
        "isRequired": false,
        "description": "Use the provided child element as the default rendered element, combining their props and behavior."
      },
      "render": {
        "type": "RenderFn<'input', PasswordInputInputState>",
        "isRequired": false,
        "description": "Render the part as a custom element, combining their props and behavior.\n\nCall `props()` to spread the part's props, passing your own to merge them rather than overwrite them.\nThe state arrives as an accessor:\n`render={(props, state) => <button {...props({ onClick: mine })}>{state().open}</button>}`"
      }
    }
  },
  "Label": {
    "props": {
      "asChild": {
        "type": "(props: PropsFn<'label'>) => Element",
        "isRequired": false,
        "description": "Use the provided child element as the default rendered element, combining their props and behavior."
      },
      "render": {
        "type": "RenderFn<'label', EmptyState>",
        "isRequired": false,
        "description": "Render the part as a custom element, combining their props and behavior.\n\nCall `props()` to spread the part's props, passing your own to merge them rather than overwrite them.\nThe state arrives as an accessor:\n`render={(props, state) => <button {...props({ onClick: mine })}>{state().open}</button>}`"
      }
    }
  },
  "Root": {
    "props": {
      "asChild": {
        "type": "(props: PropsFn<'div'>) => Element",
        "isRequired": false,
        "description": "Use the provided child element as the default rendered element, combining their props and behavior."
      },
      "autoComplete": {
        "type": "'current-password' | 'new-password'",
        "defaultValue": "\"current-password\"",
        "isRequired": false,
        "description": "The autocomplete attribute for the password input."
      },
      "defaultVisible": {
        "type": "boolean",
        "isRequired": false,
        "description": "The default visibility of the password input."
      },
      "disabled": { "type": "boolean", "isRequired": false, "description": "Whether the password input is disabled." },
      "ids": { "type": "ElementIds", "isRequired": false, "description": "The ids of the password input parts" },
      "ignorePasswordManagers": {
        "type": "boolean",
        "isRequired": false,
        "description": "When `true`, the input will ignore password managers.\n\n**Only works for the following password managers**\n- 1Password, LastPass, Bitwarden, Dashlane, Proton Pass"
      },
      "invalid": { "type": "boolean", "isRequired": false, "description": "The invalid state of the password input." },
      "name": { "type": "string", "isRequired": false, "description": "The name of the password input." },
      "onVisibilityChange": {
        "type": "(details: VisibilityChangeDetails) => void",
        "isRequired": false,
        "description": "Function called when the visibility changes."
      },
      "readOnly": { "type": "boolean", "isRequired": false, "description": "Whether the password input is read only." },
      "render": {
        "type": "RenderFn<'div', PasswordInputRootState>",
        "isRequired": false,
        "description": "Render the part as a custom element, combining their props and behavior.\n\nCall `props()` to spread the part's props, passing your own to merge them rather than overwrite them.\nThe state arrives as an accessor:\n`render={(props, state) => <button {...props({ onClick: mine })}>{state().open}</button>}`"
      },
      "required": { "type": "boolean", "isRequired": false, "description": "Whether the password input is required." },
      "translations": {
        "type": "IntlTranslations",
        "isRequired": false,
        "description": "The localized messages to use."
      },
      "visible": { "type": "boolean", "isRequired": false, "description": "Whether the password input is visible." }
    }
  },
  "RootProvider": {
    "props": {
      "value": { "type": "UsePasswordInputReturn", "isRequired": true },
      "asChild": {
        "type": "(props: PropsFn<'div'>) => Element",
        "isRequired": false,
        "description": "Use the provided child element as the default rendered element, combining their props and behavior."
      },
      "render": {
        "type": "RenderFn<'div', PasswordInputRootProviderState>",
        "isRequired": false,
        "description": "Render the part as a custom element, combining their props and behavior.\n\nCall `props()` to spread the part's props, passing your own to merge them rather than overwrite them.\nThe state arrives as an accessor:\n`render={(props, state) => <button {...props({ onClick: mine })}>{state().open}</button>}`"
      }
    }
  },
  "VisibilityTrigger": {
    "props": {
      "asChild": {
        "type": "(props: PropsFn<'button'>) => Element",
        "isRequired": false,
        "description": "Use the provided child element as the default rendered element, combining their props and behavior."
      },
      "render": {
        "type": "RenderFn<'button', PasswordInputVisibilityTriggerState>",
        "isRequired": false,
        "description": "Render the part as a custom element, combining their props and behavior.\n\nCall `props()` to spread the part's props, passing your own to merge them rather than overwrite them.\nThe state arrives as an accessor:\n`render={(props, state) => <button {...props({ onClick: mine })}>{state().open}</button>}`"
      }
    }
  }
}
