{
  "AppToastProvider": "import { Toast, type ToastProviderProps } from '@ark-ui/solid'\nimport { children } from 'solid-js'\nimport { For } from 'solid-js/web'\n\nconst AppToastProvider = (props: ToastProviderProps) => {\n  const getChildren = children(() => props.children)\n  return (\n    <Toast.Provider {...props}>\n      <Toast.Placements>\n        {(placements) => (\n          <For each={placements()}>\n            {(placement) => (\n              <Toast.Group placement={placement}>\n                {(toasts) => (\n                  <For each={toasts()}>\n                    {(toast) => (\n                      <Toast.Root toast={toast}>\n                        <Toast.Title />\n                        <Toast.Description />\n                        <Toast.CloseTrigger>Close</Toast.CloseTrigger>\n                      </Toast.Root>\n                    )}\n                  </For>\n                )}\n              </Toast.Group>\n            )}\n          </For>\n        )}\n      </Toast.Placements>\n      {getChildren()}\n    </Toast.Provider>\n  )\n}\n",
  "App": "const App = () => <AppToastProvider>{/* Your App */}</AppToastProvider>\n",
  "SimpleToast": "import { Toast, useToast } from '@ark-ui/solid'\n\nconst SimpleToast = () => {\n  const toast = useToast()\n  return (\n    <button\n      onClick={() => {\n        toast().create({\n          title: 'Hello',\n          description: 'This is a toast',\n        })\n      }}\n    >\n      Add Toast\n    </button>\n  )\n}\n",
  "ConfigureToast": "import { Toast, useToast } from '@ark-ui/solid'\n\nconst ConfigureToast = () => {\n  const toast = useToast()\n  return (\n    <button\n      onClick={() => {\n        toast().create({\n          title: 'Success',\n          description: 'This is a success toast',\n          type: 'success',\n          placement: 'bottom-start',\n          duration: 20000,\n          removeDelay: 250,\n        })\n      }}\n    >\n      Add Toast\n    </button>\n  )\n}\n",
  "CustomRenderToast": "import { Toast, useToast } from '@ark-ui/solid'\n\nconst CustomRenderToast = () => {\n  const toast = useToast()\n  return (\n    <button\n      onClick={() => {\n        toast().create({\n          title: 'Please checkout',\n          render: (toast) => (\n            <div>\n              {toast.title} <a href=\"https://ark-ui.com\">Ark UI</a>\n            </div>\n          ),\n        })\n      }}\n    >\n      Add Toast\n    </button>\n  )\n}\n",
  "DefaultOptions": "const DefaultOptions = () => (\n  <AppToastProvider defaultOptions={{ duration: 2000, placement: 'top-end', removeDelay: 250 }}>\n    {/* ... */}\n  </AppToastProvider>\n)\n"
}
