---
import type { CollectionEntry } from 'astro:content'
import { ComponentPropsTable } from './component-props-table'

type Props = {
  types: CollectionEntry<'types'>
}

const { types } = Astro.props
---

<ComponentPropsTable types={types} client:load />
