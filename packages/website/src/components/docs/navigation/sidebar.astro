---
import { Stack } from 'styled-system/jsx/stack'
import { getSitemap } from '~/lib/page-control'
import { SidebarNavigation } from './sidebar-navigation'

const items = await getSitemap()
---

<Stack gap="6">
  <SidebarNavigation pathname={Astro.url.pathname} items={items} client:load transition:persist />
 </Stack>
