import { describe, expect, it } from 'vitest'

describe('@ark-ui/angular scaffold', () => {
  it('keeps the package test gate active before component specs exist', () => {
    expect(true).toBe(true)
  })
})
